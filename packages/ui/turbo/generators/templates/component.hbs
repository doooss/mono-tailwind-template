import { ReactNode } from 'react';

interface Props {
    children?: ReactNode;
}

const {{ pascalCase name }} = ({ children }: Props) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default {{ pascalCase name }};
