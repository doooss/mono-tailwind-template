import { ReactNode } from 'react';

interface Props {
    children?: ReactNode;
}

const {{ pascalCase name }} = ({ children }: Props) => {
    return (
        <div>
            <h1>{{ name }}</h1>
            {children}
        </div>
    );
};

export default {{ pascalCase name }};
